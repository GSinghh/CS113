#pragma once
//This is the header file which will include the functions
class logicGates
{
	bool T;
	bool F;
} 
bool conjunction(logicGates);
bool disjunction(logicGates);
bool exclusiveDisjunction(logicGates);
